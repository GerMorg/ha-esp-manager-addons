# ESP Manager Update 0.12.1
Diese Ordnerstruktur über den gleichnamigen Repository-Pfaden einfügen. `esp_manager` Stable bleibt unverändert. Danach ESP Manager Dev neu bauen und betroffene ESP-Projekte neu kompilieren.
Allgemeine API: `registerSensor`, `registerBinarySensor`, `registerSwitch`, `registerNumber`, `registerCover`, `onCommand`, `publishState`.
