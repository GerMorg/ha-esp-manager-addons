schema_version: 1
required:
  hardware_page: [manifest_check_retry, usb_flash, serial_monitor, mqtt_status, device_status, device_history, ota, forget_device]
  ui: [responsive, output_wrap, collapsible_device_details]
  agent_api: [begin, loop, log, publishSensor, handleCommand]
