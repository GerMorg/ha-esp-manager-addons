schema_version: 1
required:
  discovery: [sensor, binary_sensor, switch, number, cover, retained_config, availability]
  control: [command_callback, state_confirmation, cover_position]
  compatibility: [publishSensor, ota, wifi_recovery]
