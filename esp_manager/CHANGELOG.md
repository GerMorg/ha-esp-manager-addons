# Changelog

## 0.6.1
- Oberfläche nach dem Wechsel auf die saubere 0.6-Codebasis repariert.
- JavaScript-Syntaxfehler durch falsch eingebettete Zeilenumbrüche behoben.
- Projektliste und vorhandene Dev-Projekte werden wieder geladen.
- Neue Projekte können wieder angelegt werden.
- Speichern der Bibliotheks- und Build-Flag-Listen funktioniert wieder.
- Zusätzlich zur Python-Prüfung wird nun auch das tatsächlich ausgelieferte JavaScript syntaktisch geprüft.
- Datenpfade bleiben unverändert: Dev verwendet weiterhin `/config/esp_manager_dev`, Stable `/config/esp_manager`.
